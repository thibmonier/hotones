# Codebase Scan Report — HotOnes

> **Status**: DRAFT — generated by `/project:scan`
> **Generated**: 2026-05-04
> **Scope**: full repository, branch `chore/housekeeping-001-doc-bundle`
> **Reviewer agent**: `@symfony-reviewer`

---

## 1. Project Identity

| Field | Value |
|-------|-------|
| Name | HotOnes |
| Repository | `thibmonier/hotones` |
| License | CC BY-NC-SA 4.0 (proprietary `composer.json`) |
| Description (README) | Plateforme SaaS multi-tenant de gestion de rentabilité pour agences web. Analyse rentabilité en croisant ventes (jours/TJM), temps passés, coûts (CJM), achats et KPIs consolidés. Multi-sociétés avec isolation complète des données. |
| Quality dashboards | SonarCloud, Snyk, GitHub Actions CI |
| Sprint cadence | BMAD v6, sprint courant `sprint-005` (Test Stabilization & Tech-Debt) |

---

## 2. Technology Stack

| Component | Technology | Version |
|-----------|------------|---------|
| Language | PHP | 8.5 (`composer.json` requires `>=8.5`) |
| Framework | Symfony | 8.0.1 (bundles `^8.0.4`) |
| ORM | Doctrine ORM | 3.6.x + DBAL 4.4 |
| Migrations | doctrine/doctrine-migrations-bundle | ^4 |
| Database | MariaDB | 11.4 (Docker) |
| Cache / queue | Redis | 7-alpine (Docker) |
| API | API Platform | 4.2.15 (REST + OpenAPI) |
| Admin UI | EasyAdmin | 5 |
| Auth | `lexik/jwt-authentication-bundle` 3.2 + `scheb/2fa-bundle` 8.3 (TOTP) |
| Frontend | Twig + Stimulus + Symfony UX (Live, Turbo, TwigComponent), Bootstrap 5 (theme Skote) |
| Asset pipeline | Symfony AssetMapper + Webpack Encore + Sass |
| Storage | `oneup/flysystem-bundle` + `league/flysystem-aws-s3-v3` |
| Search / paginate | KnpPaginatorBundle 6.10 |
| Mailer / notifier | symfony/mailer, symfony/notifier |
| Messenger | symfony/doctrine-messenger + symfony/redis-messenger |
| Scheduler | symfony/scheduler + cron-expression |
| Observability | sentry/sentry-symfony 5.8, monolog-bundle 4.0 |
| Security headers | `nelmio/security-bundle` 3.8 (CSP, etc.) |
| AI | symfony/ai-bundle (Anthropic, OpenAI, Gemini), `anthropic-ai/sdk`, `openai-php/client` |
| PDF | `dompdf/dompdf` 3.1 |
| Spreadsheet | `phpoffice/phpspreadsheet` 5.4 |
| Imaging | `liip/imagine-bundle` 2.17 + Endroid QR Code |
| External CRM | HubSpot, BoondManager (custom adapters in `src/Service`) |
| Markdown | league/commonmark 2.8 |

---

## 3. Repository Layout

```
src/
├── AI/                       # AI tooling (Anthropic, OpenAI, Gemini)
├── ApiResource/              # API Platform resources (legacy alongside Entity)
├── Application/              # DDD Application layer (use cases, commands, queries)
│   ├── Catalog/
│   ├── Client/
│   ├── Notification/
│   ├── Reservation/          # ⚠️ likely template/sample BC
│   └── Vacation/
├── Command/                  # 46 console commands
├── Components/               # Symfony UX TwigComponents
├── Controller/               # 80 controllers (mixed legacy + DDD presentation)
│   ├── Admin/                # EasyAdmin CRUD + Backoffice
│   ├── Analytics/
│   ├── Api/
│   ├── Security/             # CspReport, Login, 2FA
│   └── Staffing/
├── DataFixtures/             # Demo data + reference data loader
├── Domain/                   # DDD Domain layer
│   ├── Catalog/
│   ├── Notification/
│   ├── Reservation/
│   └── Vacation/
├── DTO/
├── Entity/                   # 63 Doctrine entities (legacy module)
├── Enum/                     # 3 enums
├── Event/                    # 8 domain/application events
├── EventListener/
├── EventSubscriber/
├── Exception/
├── Factory/
├── Form/
├── Infrastructure/           # DDD Infrastructure layer
│   ├── Cache/
│   ├── EventBus/
│   ├── Http/
│   ├── Notification/
│   ├── Persistence/
│   └── Vacation/
├── Menu/
├── Message/
├── MessageHandler/           # 7 async handlers
├── Monolog/Processor/
├── Presentation/             # DDD Presentation layer (Vacation, etc.)
├── Repository/               # 59 Doctrine repositories
├── Scheduler/
├── Security/Voter/           # ⚠️ only 1 voter — coverage gap
├── Service/                  # 48 services
│   ├── AI/
│   ├── Analytics/
│   ├── BoondManager/
│   ├── HubSpot/
│   ├── Order/
│   ├── Planning/
│   └── Timesheet/
├── Story/                    # Foundry story factories
└── Twig/
tests/
├── Api/
├── Contract/
├── E2E/                      # Panther
├── Functional/               # 17 files
├── Integration/              # 22 files
├── Security/
├── Support/
└── Unit/                     # 48 files
config/packages/              # 27 bundle configs
docker/                       # mysql, nginx, php, scripts, supervisor
.github/workflows/            # 9 pipelines
migrations/                   # 14 active migrations
migrations_archive/           # legacy migrations
.bmad/                        # sprint-status.yaml + batch-queue.yaml
project-management/           # BMAD backlog + sprints (5 done)
```

> **Architectural observation**: hybrid layout. Legacy "Symfony classic" tree (`Entity/Controller/Repository/Service`) coexists with DDD layers (`Domain/Application/Infrastructure/Presentation`). Migration path in progress on bounded contexts `Vacation`, `Catalog`, `Notification`, `Reservation`. New code expected under DDD layers per `.claude/rules/02-architecture-clean-ddd.md`.

---

## 4. Bounded Contexts / Modules

### 4.1 DDD layers (`src/Domain` + `src/Application`)

| Context | Domain dirs | Application dirs | Status |
|---------|-------------|------------------|--------|
| **Catalog** | Entity, Event, Exception, Repository, Service, ValueObject | Query, UseCase | active |
| **Notification** | Event, Exception, Service, ValueObject | UseCase | partial |
| **Reservation** | Entity, Event, Exception, Repository, Service, ValueObject | Query (GetReservationDetails, ListReservations), UseCase (Create/Confirm/Cancel), EventHandler | ⚠️ likely template — verify if real domain |
| **Vacation** | Entity, Event, Exception, Repository, ValueObject | Command (Approve/Cancel/Reject/Request), Query (Count, ContributorVacations, PendingForManager), DTO, Notification/Message | active (CQRS) |

### 4.2 Legacy modules (inferred from `src/Entity` + `src/Controller`)

| Module | Anchor entities | Anchor controllers |
|--------|----------------|--------------------|
| **Identity & Access** | `User`, `Profile`, `Company`, `CompanySettings`, `BusinessUnit`, `EmployeeLevel` | `SecurityController`, `TwoFactorController`, `ProfileController`, `AdminUserController` |
| **CRM / Sales pipeline** | `Client`, `ClientContact`, `LeadCapture`, `Provider`, `Vendor` | `ClientController`, `ClientContactController`, `CrmLeadController`, `LeadMagnetController`, `SalesDashboardController` |
| **Orders & Quotes** | `Order`, `OrderLine`, `OrderSection`, `OrderTask`, `OrderPaymentSchedule` | `OrderController`, `Service/Order/*` |
| **Project delivery** | `Project`, `ProjectTask`, `ProjectSubTask`, `ProjectEvent`, `ProjectSkill`, `ProjectTechnology`, `ProjectHealthScore` | `ProjectController`, `ProjectDetailController`, `ProjectTaskController`, `ProjectSubTaskController`, `ProjectHealthController`, `ProjectTechnologyController`, `RiskController` |
| **Planning & Staffing** | `Planning`, `PlanningSkill`, `RunningTimer`, `EmploymentPeriod` | `PlanningController`, `PlanningOptimizationController`, `StaffingDashboardController`, `WorkloadPredictionController`, `MyTasksController` |
| **Timesheet & Treasury** | `Timesheet`, `RunningTimer`, `BillingMarker`, `FactForecast` | `TimesheetController`, `TreasuryController`, `BillingController`, `ProjectBillingController` |
| **Invoicing** | `Invoice`, `InvoiceLine`, `ExpenseReport` | `InvoiceController`, `ExpenseReportController` |
| **HR & Performance** | `Contributor`, `ContributorSkill`, `ContributorTechnology`, `ContributorSatisfaction`, `ContributorProgress`, `PerformanceReview`, `OnboardingTask`, `OnboardingTemplate`, `Skill`, `Technology` | `ContributorController`, `ContributorSatisfactionController`, `ContributorSkillController`, `ContributorTechnologyController`, `EmploymentPeriodController`, `PerformanceReviewController`, `HrDashboardController`, `OnboardingController`, `MyTasksController` |
| **Gamification** | `Achievement`, `Badge`, `XpHistory` | `BadgeController`, `LeaderboardController` |
| **NPS & feedback** | `NpsSurvey` | `NpsController`, `NpsPublicController` |
| **SaaS catalog & subscriptions** | `SaasProvider`, `SaasService`, `SaasSubscription`, `Subscription`, `ServiceCategory` | `SaasController`, `SubscriptionController` |
| **Content & marketing** | `BlogPost`, `BlogCategory`, `BlogTag` | `BlogController`, `BlogPostCrudController`, `BlogCategoryCrudController`, `BlogTagCrudController` |
| **Notifications** | `Notification`, `NotificationPreference`, `NotificationSetting` | `NotificationController` |
| **Compliance / GDPR** | `AccountDeletionRequest`, `CookieConsent` | `GdprController`, `CspReportController` |
| **External integrations** | `BoondManagerSettings`, `HubSpotSettings` | `BoondManagerSettingsController`, `HubSpotSettingsController`, `Service/BoondManager/*`, `Service/HubSpot/*` |
| **AI** | (no entities) | `ChatbotController`, `Service/AI/*`, `AI/Tool/*` |
| **Analytics & forecasting** | `FactForecast`, `ProjectHealthScore` | `Controller/Analytics/*`, `ForecastingController`, `PredictionsController` |
| **Scheduler / batch** | `SchedulerEntry` | `Schedule.php`, `Scheduler/*`, `SchedulerEntryCrudController` |

### 4.3 Counts summary

| Asset | Count |
|-------|-------|
| Doctrine entities (`src/Entity`) | 63 |
| Repositories (`src/Repository`) | 59 |
| Controllers (`src/Controller/**`) | 80 |
| Services (`src/Service/**`) | 48 |
| Console commands (`src/Command`) | 46 |
| Message handlers (`src/MessageHandler`) | 7 |
| Domain events (`src/Event`) | 8 |
| Security voters (`src/Security/Voter`) | 1 ⚠️ |
| Enums (`src/Enum`) | 3 |
| Twig components (`src/Components`) | n/a (dir present) |
| Doctrine migrations (active) | 14 |

---

## 5. HTTP Surface (Endpoints)

### 5.1 Volume

`#[Route(...)]` declarations across `src/Controller/**`: **338** route attributes for **80** controllers.

> Full per-controller route listing not enumerated in this report. Run `bin/console debug:router` for canonical list.

### 5.2 Public access (from `config/packages/security.yaml` `access_control`)

| Path prefix | Visibility |
|-------------|------------|
| `/`, `/login`, `/2fa` | PUBLIC_ACCESS |
| `/features`, `/pricing`, `/about`, `/contact`, `/legal` | PUBLIC_ACCESS (marketing) |
| `/blog`, `/public` | PUBLIC_ACCESS |
| `/sitemap`, `/robots.txt`, `/health`, `/status` | PUBLIC_ACCESS |
| `/csp/report`, `/csp/violations` | PUBLIC_ACCESS (CSP reporting) |
| `/api/login`, `/api/docs` | PUBLIC_ACCESS |
| `/api/**` (rest) | `ROLE_USER` (JWT) |
| `/**` (rest) | `ROLE_USER` (form login) |

### 5.3 API surface

- API Platform 4.2 mounted under `/api`
- OpenAPI docs at `/api/docs` and `/api/documentation`
- JWT auth via Lexik (`/api/login`)
- Custom REST controllers in `src/Controller/Api/` (e.g. `TaskApiController`, `DependentFieldsController`, `PredictionsController`, `DashboardController`)

### 5.4 Admin / backoffice

- EasyAdmin dashboard at `/backoffice` (ROLE_ADMIN)
- 14+ CrudControllers detected (`*CrudController.php`)

---

## 6. Security Model

### 6.1 Role hierarchy (from `security.yaml`)

```
ROLE_SUPERADMIN ─┬─ ROLE_ADMIN ─┬─ ROLE_MANAGER ─┬─ ROLE_CHEF_PROJET ─┬─ ROLE_INTERVENANT ─ ROLE_USER
                 │              │                │                    │
                 └─ ROLE_COMPTA ─┘                │                    │
                                                  └────────────────────┘
```

Additional role detected in code only (not in hierarchy): `ROLE_COMMERCIAL` ⚠️ verify whether dead code or missing config.

### 6.2 Authentication

| Mechanism | Component | Notes |
|-----------|-----------|-------|
| Form login | symfony/security-bundle | session |
| 2FA TOTP | scheb/2fa-bundle 8.3 + scheb/2fa-totp | `TwoFactorController` |
| API JWT | lexik/jwt-authentication-bundle 3.2 | RS256 keys generated in CI |
| CSRF | symfony/security-csrf | enabled (`csrf.yaml`) |

### 6.3 Authorization

- 1 voter only (`src/Security/Voter`) — **coverage gap** vs 80 controllers.
- Most authorization relies on `access_control` paths + `IsGranted` annotations (not measured here).
- Per `.claude/rules/11-security-symfony.md`, voters are the recommended pattern for entity-level access; current state is below standard.

### 6.4 Hardening

| Concern | Implementation |
|---------|----------------|
| CSP | `nelmio/security-bundle` + `CspReportController` collecting violations |
| Rate limiting | `symfony/rate-limiter` (`config/packages/rate_limiter.yaml`) |
| HTML sanitizer | `symfony/html-sanitizer` |
| Security advisories | `roave/security-advisories: dev-master` (composer plugin) |
| SAST / SCA | Snyk (`.snyk`, `snyk.yml` workflow), SonarCloud |
| Vulnerability dashboards | Sentry + Sonar badges in README |

### 6.5 Multi-tenancy

- README: "Support multi-sociétés avec isolation complète des données"
- Anchor entities: `Company`, `BusinessUnit`, `CompanySettings`, `Contributor` (each scoped per tenant — to verify in `Company` mapping)
- Tenant filter implementation **not located via this scan** — should be checked against `.claude/rules/14-multitenant.md` (Doctrine SQLFilter pattern).

---

## 7. Persistence

| Aspect | Detail |
|--------|--------|
| ORM | Doctrine ORM 3.6 |
| Mapping style | Attributes on entities (legacy) — DDD entities should not carry Doctrine attributes (verify in `Domain/*/Entity`) |
| Migrations | 14 active in `migrations/` + `migrations_archive/` |
| Doctrine extensions | `gedmo/doctrine-extensions` ≥3.21 + `stof/doctrine-extensions-bundle` 1.15 (Timestampable, Sluggable, Blameable, SoftDeleteable, Translatable, Loggable available — usage to be confirmed per entity) |
| Custom DBAL | `beberlei/doctrineextensions` (extra functions) |

---

## 8. Asynchronous & Scheduled Work

| Feature | Implementation |
|---------|----------------|
| Message bus | symfony/messenger |
| Transports | Doctrine messenger (failed) + Redis messenger (async) |
| Handlers | 7 in `src/MessageHandler` |
| Scheduler | symfony/scheduler + `Schedule.php` + `src/Scheduler` + `SchedulerEntry` entity (cron-expression) |
| AI workloads | symfony/ai-bundle + `Service/AI` + `AI/Tool` |

---

## 9. Tests

### 9.1 Suite breakdown (from `phpunit.xml.dist`)

| Suite | Files (PHP) |
|-------|-------------|
| Unit | 48 |
| Integration | 22 |
| Functional | 17 |
| E2E (Panther) | 4 |
| Api / Contract / Security / Support | remainder (~11) |
| **Total test files** | **102** |

> Code coverage % not measured by this scan. Reference value: SonarCloud badge in `README.md`.

### 9.2 Frameworks

- PHPUnit (Symfony PHPUnit Bridge)
- DAMA Doctrine Test Bundle (transactional rollback per test)
- symfony/panther (E2E)
- Foundry-style factories under `src/Story/`

### 9.3 Active sprint context

`sprint-005-test-stabilization` (2026-05-04 → 2026-05-15) — goal: "Stabiliser la suite de tests fonctionnels (Vacation + connectors), eliminer la dette mockObjects et fiabiliser le pre-push hook pour ne plus recourir a `--no-verify`". Capacity 32 pts.

History (delivered = planned for 001-003):

| Sprint | Name | Velocity |
|--------|------|----------|
| sprint-001 | Walking Skeleton | 21 |
| sprint-002 | Tests Consolidation | 34 |
| sprint-003 | Stabilization | 30 |
| sprint-004 | Quality Foundation | (in `sprints/sprint-004-quality-foundation/`) |
| sprint-005 | Test Stabilization & Tech-Debt | 32 (in flight) |
| sprint-006 | Test Debt Cleanup | (in `sprints/sprint-006-test-debt-cleanup/`) |

---

## 10. Infrastructure & DevOps

### 10.1 Docker

`docker-compose.yml` services:

| Service | Image | Container |
|---------|-------|-----------|
| `app` | local build (Dockerfile) | `hotones_app` |
| `web` | `nginx:1.27-alpine` | `hotones_web` |
| `db` | `mariadb:11.4` | `hotones_db` |
| `redis` | `redis:7-alpine` | `hotones_redis` |

Docker support directories: `docker/{mysql,nginx,php,scripts,supervisor}`.

### 10.2 GitHub Actions workflows (`.github/workflows/`)

| Workflow | Purpose |
|----------|---------|
| `ci.yml` | Composer cache, PHPUnit, E2E (Panther) |
| `ci-health-check.yml` | Pipeline health monitoring |
| `quality.yml` | PHPStan / CS-Fixer / Rector / etc. (TBC) |
| `contract-tests.yml` | API contract tests |
| `pr-ci-comment.yml` | Bot comment on PRs |
| `snyk.yml` | SCA / vulnerability scan |
| `sonarqube.yml` | SonarCloud upload |
| `staging-backup.yml` | Staging DB backup |
| `staging-smoke-test.yml` | Post-deploy smoke tests |

### 10.3 Quality dashboards (README badges)

- SonarCloud (Quality Gate, Coverage, Security/Maintainability/Reliability ratings, Bugs, Vulnerabilities, Code Smells, Technical Debt, SQALE Index)
- Snyk (vulnerability scanning)
- GitHub Actions CI status

### 10.4 Observability

- Sentry (`sentry/sentry-symfony` 5.8) — exception + performance tracing
- Monolog (`monolog-bundle` 4.0) + custom processor `src/Monolog/Processor`
- HealthCheckController, StatusController exposed publicly

### 10.5 External integrations

| Provider | Component |
|----------|-----------|
| HubSpot | `Service/HubSpot/*` + `HubSpotSettings` entity + `HubSpotSettingsController` |
| BoondManager | `Service/BoondManager/*` + `BoondManagerSettings` entity + `BoondManagerSettingsController` |
| AWS S3 | `oneup/flysystem-bundle` + `league/flysystem-aws-s3-v3` |
| Anthropic / OpenAI / Gemini | `symfony/ai-*-platform` packages + bundle |

---

## 11. Architectural Patterns

| Pattern | Evidence |
|---------|----------|
| **Clean Architecture / Hexagonal** | `Domain` / `Application` / `Infrastructure` / `Presentation` layers (DDD bounded contexts) |
| **CQRS** | `Application/Vacation/{Command,Query}`, `Application/Reservation/{UseCase,Query}` |
| **Domain Events** | `src/Event` (8 events) + DDD `Domain/*/Event` |
| **Strategy / Adapter** | `Service/AI/*` (multi-provider AI), `Service/{HubSpot,BoondManager}/*` |
| **Repository pattern** | 59 repositories under `src/Repository` |
| **Voter pattern** | declared but only 1 voter (gap) |
| **Symfony UX components** | `src/Components` (Twig + Stimulus) |
| **Scheduler / cron** | symfony/scheduler + `SchedulerEntry` |
| **Multi-tenant isolation** | tenant entity = `Company` (mechanism not yet located) |

---

## 12. Code Quality Tooling (composer + repo)

> Detailed config not opened in this pass.

| Tool | Notes |
|------|-------|
| PHPStan | `.deptrac.cache` present + `.bmad/quality.yml` workflow → assumed configured |
| PHP-CS-Fixer | `.php-cs-fixer.dist.php` + `.php-cs-fixer.cache` |
| Rector | not confirmed in this pass |
| Deptrac | `.deptrac.cache` present |
| pre-commit hooks | `.githooks/` directory + `pre-commit-check` skill |
| SonarCloud | active (badges) |
| Snyk | active (`.snyk`, workflow) |

---

## 13. Documentation Surface

| Source | Coverage |
|--------|----------|
| `README.md` | quickstart, stack, URLs, key commands |
| `AGENTS.md` | agent guidance |
| `WARP.md` | (referenced by README badge) |
| `docs/` | `mago.md`, `analysis-property-hooks-migration.md`, `multi-tenant-company-context-migration.md`, `performance.md` |
| `project-management/` | BMAD backlog index, sprint folders 001–006 with `sprint-goal`, `task-board`, retro/review |
| `.bmad/sprint-status.yaml` | active sprint metadata + history |
| **No PRD / no formal requirements doc** | ⚠️ greenfield reverse-engineering needed |

---

## 14. Known Gaps & Risks (from this scan)

| Severity | Item | Evidence |
|----------|------|----------|
| 🔴 high | Authorization coverage thin: 1 voter for 80 controllers / 63 entities | `src/Security/Voter` count |
| 🔴 high | `ROLE_COMMERCIAL` referenced in code but absent from `role_hierarchy` | `grep ROLE_` vs `security.yaml` |
| 🟠 med | Dual architecture (legacy `Entity/` vs DDD `Domain/`) — partial migration, risk of drift | `src/` layout |
| 🟠 med | Multi-tenant filter implementation not located by scan; needs verification vs `.claude/rules/14-multitenant.md` | absent SQLFilter under `Infrastructure` |
| 🟠 med | `Application/Reservation/*` may be sample/template not aligned with HotOnes domain (Reservation not mentioned in README) | folder vs README |
| 🟠 med | Test mockObjects debt explicit in sprint-005 goal | `.bmad/sprint-status.yaml` |
| 🟡 low | API Platform resources: `src/ApiResource` directory exists but empty per `ls *.php` — verify mapping strategy (attributes on entities?) | `ls src/ApiResource/*.php` returns 0 files |
| 🟡 low | 14 active migrations + `migrations_archive/` — confirm archive compatibility before fresh installs | folder presence |
| 🟡 low | PHP 8.5 minimum in `composer.json` while ecosystem still rolling out — confirm CI runs against 8.5 | `composer.json` `require.php` |

---

## 15. Next Steps

1. ✅ `/project:scan` — done (this report).
2. 🔜 `/project:reverse-prd` — generate DRAFT PRD from this inventory.
3. 🔜 `/project:reverse-stories` — generate user stories per module (recommend prioritising Project / Order / Timesheet / Vacation / Invoice / HR Dashboard).
4. 🔜 `/project:gap-analysis` — compare DRAFT PRD vs actual code to surface missing requirements.

---

> **Confidence**: HIGH for stack/structure/counts, MEDIUM for module groupings (legacy module names inferred from controller/entity naming, not domain glossary), LOW for multi-tenant isolation mechanism (not located in this pass).
