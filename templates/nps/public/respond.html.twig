<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enquête de Satisfaction</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .survey-card {
            max-width: 700px;
            width: 100%;
        }
        .nps-buttons {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }
        .nps-buttons input[type="radio"] {
            display: none;
        }
        .nps-buttons label {
            flex: 1;
            min-width: 45px;
            padding: 15px 10px;
            text-align: center;
            border: 2px solid #dee2e6;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
            background: white;
            font-weight: 600;
        }
        .nps-buttons label:hover {
            border-color: #667eea;
            transform: translateY(-2px);
        }
        .nps-buttons input[type="radio"]:checked + label {
            border-color: #667eea;
            background: #667eea;
            color: white;
        }
        .score-legend {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
            font-size: 0.875rem;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="survey-card">
        <div class="card shadow-lg border-0">
            <div class="card-body p-5">
                <div class="text-center mb-4">
                    <h2 class="fw-bold mb-3">Enquête de Satisfaction</h2>
                    <p class="text-muted">Projet : <strong>{{ survey.project.name }}</strong></p>
                </div>

                {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

                    <div class="mb-4">
                        <label class="form-label fw-bold">
                            Sur une échelle de 0 à 10, quelle est la probabilité que vous recommandiez nos services ?
                        </label>
                        <div class="nps-buttons">
                            {% for choice in form.score.vars.choices %}
                                <input type="radio"
                                       name="{{ form.score.vars.full_name }}"
                                       id="score_{{ choice.value }}"
                                       value="{{ choice.value }}"
                                       required>
                                <label for="score_{{ choice.value }}">{{ choice.value }}</label>
                            {% endfor %}
                        </div>
                        <div class="score-legend">
                            <span>Pas du tout probable</span>
                            <span>Extrêmement probable</span>
                        </div>
                        {{ form_errors(form.score) }}
                    </div>

                    <div class="mb-4">
                        {{ form_label(form.comment, 'Pouvez-vous nous expliquer votre note ? (optionnel)', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.comment, {'attr': {'class': 'form-control', 'rows': 5, 'placeholder': 'Partagez votre expérience avec nous...'}}) }}
                        {{ form_errors(form.comment) }}
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">
                            Envoyer ma réponse
                        </button>
                    </div>

                    <div style="display: none;">
                        {{ form_rest(form) }}
                    </div>

                {{ form_end(form, {'render_rest': false}) }}

                <div class="text-center mt-4">
                    <p class="text-muted small mb-0">
                        Vos réponses sont confidentielles et nous aident à améliorer nos services.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
