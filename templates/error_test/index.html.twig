{% extends 'layouts/base.html.twig' %}
{% block title %}Test des pages d'erreur{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="page-title-box d-flex align-items-center justify-content-between">
                <h4 class="mb-0">Test des pages d'erreur</h4>
                <div class="page-title-right">
                    <ol class="breadcrumb m-0">
                        <li class="breadcrumb-item"><a href="{{ path('home') }}">Accueil</a></li>
                        <li class="breadcrumb-item active">Test des pages d'erreur</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title mb-4">
                        <i class="bx bx-bug me-2"></i> Pages d'erreur disponibles
                    </h5>
                    <div class="alert alert-warning" role="alert">
                        <i class="bx bx-info-circle me-2"></i>
                        <strong>Attention :</strong> Ces liens génèrent volontairement des erreurs pour tester l'affichage des pages d'erreur personnalisées.
                    </div>

                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <div class="card border-primary">
                                <div class="card-body text-center">
                                    <h1 class="text-primary mb-3">404</h1>
                                    <h6 class="card-title">Page non trouvée</h6>
                                    <p class="card-text text-muted">Test de la page d'erreur 404</p>
                                    <a href="{{ path('test_errors_404') }}" class="btn btn-primary">
                                        <i class="bx bx-bug me-1"></i> Tester 404
                                    </a>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-4 mb-3">
                            <div class="card border-danger">
                                <div class="card-body text-center">
                                    <h1 class="text-danger mb-3">403</h1>
                                    <h6 class="card-title">Accès interdit</h6>
                                    <p class="card-text text-muted">Test de la page d'erreur 403</p>
                                    <a href="{{ path('test_errors_403') }}" class="btn btn-danger">
                                        <i class="bx bx-shield-x me-1"></i> Tester 403
                                    </a>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-4 mb-3">
                            <div class="card border-warning">
                                <div class="card-body text-center">
                                    <h1 class="text-warning mb-3">500</h1>
                                    <h6 class="card-title">Erreur serveur</h6>
                                    <p class="card-text text-muted">Test de la page d'erreur 500</p>
                                    <a href="{{ path('test_errors_500') }}" class="btn btn-warning">
                                        <i class="bx bx-error-circle me-1"></i> Tester 500
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="alert alert-info mt-4" role="alert">
                        <h6 class="alert-heading">
                            <i class="bx bx-info-circle me-2"></i> Comment tester en production ?
                        </h6>
                        <p class="mb-0">
                            En production (APP_ENV=prod), les pages d'erreur personnalisées s'afficheront automatiquement.
                            Pour forcer l'affichage en mode debug, vous pouvez utiliser :
                        </p>
                        <code class="d-block mt-2">/_error/{statusCode}</code>
                        <p class="mb-0 mt-2">
                            Exemple : <code>/_error/404</code>, <code>/_error/403</code>, <code>/_error/500</code>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
