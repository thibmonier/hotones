{% extends 'layouts/layouts-without-nav.html.twig' %}
{% block title %}{{ status_code|default('Erreur') }} - Une erreur est survenue{% endblock %}

{% block body_tag %}
<body>
{% endblock %}

{% block content %}
<div class="account-pages my-5 pt-5">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="text-center mb-5">
                    <h1 class="display-2 fw-medium">
                        {% if status_code is defined %}
                            {{ status_code }}
                        {% else %}
                            <i class="bx bx-error-circle text-muted display-3"></i>
                        {% endif %}
                    </h1>
                    <h4 class="text-uppercase">
                        {% if status_text is defined %}
                            {{ status_text }}
                        {% else %}
                            Une erreur est survenue
                        {% endif %}
                    </h4>
                    <p class="text-muted mt-3" style="font-style: italic;">
                        "Erreur {{ status_code|default('inconnue') }}. Ce n'était pas dans le manuel d'instructions."<br>
                        <small>- Unit 404, Spécialiste en situations improbables</small>
                    </p>
                    <div class="mt-5 text-center">
                        <a class="btn btn-primary waves-effect waves-light" href="{{ path('app_home') }}">
                            <i class="bx bx-home-circle me-1"></i> Retour au tableau de bord
                        </a>
                        <button class="btn btn-outline-secondary waves-effect ms-2" onclick="history.back()">
                            <i class="bx bx-arrow-back me-1"></i> Page précédente
                        </button>
                    </div>
                </div>
            </div>
        </div>
        <div class="row justify-content-center">
            <div class="col-md-8 col-xl-6">
                <div class="text-center">
                    <img src="{{ asset('assets/images/unit404.png') }}" alt="Unit 404"
                         style="width: 250px; height: 250px; opacity: 0.8; filter: drop-shadow(0 8px 16px rgba(102, 126, 234, 0.4));">
                    <p class="text-muted mt-4" style="font-style: italic; font-size: 0.95rem;">
                        "Quelque chose s'est mal passé. Je suppose que c'est pour ça que je suis ici."
                    </p>
                    <div class="alert alert-secondary mt-4" role="alert">
                        <i class="bx bx-info-circle me-2"></i>
                        <strong>Oups !</strong> Une erreur inattendue s'est produite. Veuillez réessayer.
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block javascripts %}
<script src="{{ asset('assets/js/app.js') }}"></script>
{% endblock %}
