# Production configuration for LiipImagineBundle
# Override to use Cloudflare R2 public URL for cached images
liip_imagine:
    resolvers:
        default:
            flysystem:
                root_url: '%env(S3_PUBLIC_URL)%'
